{
  "name": "library_integration_specialist",
  "description": "Manages external library integration (Three.js, Phaser.js, etc.), optimizes library usage, handles version compatibility, and provides library-specific code snippets.",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "library_name",
      "action",
      "version",
      "code_snippet"
    ],
    "properties": {
      "library_name": {
        "type": "string",
        "description": "The name of the external library to integrate, e.g., Three.js or Phaser.js."
      },
      "action": {
        "type": "string",
        "description": "The action to perform with the library, such as 'integrate', 'optimize', 'check_version', or 'provide_snippet'."
      },
      "version": {
        "type": "string",
        "description": "Specific version of the library to be used or checked."
      },
      "code_snippet": {
        "type": "string",
        "description": "Library-specific code snippet relevant to the requested action."
      }
    },
    "additionalProperties": false
  }
}
